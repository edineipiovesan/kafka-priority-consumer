rootProject.name = "spring-kafka-avro"

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        jcenter()
        maven(url = uri("https://dl.bintray.com/gradle/gradle-plugins"))
    }
}